# Geotab MyGeotab API Credentials
# Get these from your MyGeotab account or the competition simulator
GEOTAB_DATABASE=your_database_name
GEOTAB_USERNAME=your_email@example.com
GEOTAB_PASSWORD=your_password
GEOTAB_SERVER=my.geotab.com

# Optional: Data Connector credentials (defaults to same as above)
# GEOTAB_DC_USERNAME=
# GEOTAB_DC_PASSWORD=
# GEOTAB_DC_DATABASE=

# Optional: DuckDB cache path (defaults to ./fleet_cache.duckdb)
# DUCKDB_PATH=./fleet_cache.duckdb

# Google Gemini AI — for fleet analysis (get key at https://aistudio.google.com/apikey)
GEMINI_API_KEY=your_gemini_api_key

# Google Maps — for dashboard map + geocoding (enable Maps JS API + Geocoding API)
# Get key at https://console.cloud.google.com/apis/credentials
GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Optional: Dashboard port (defaults to 5001)
# DASHBOARD_PORT=5001
